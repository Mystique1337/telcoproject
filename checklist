  ---
  1. Project Setup
  - Scaffold frontend_v2 — Vite + React + TypeScript
  - Install and configure Tailwind + shadcn/ui
  - Set up React Router v6, TanStack Query, Zustand
  - Create Supabase project + connect to FastAPI backend

  2. Database (Supabase)
  - organizations table
  - users table
  - projects table (name, product description, category, image)
  - panel_runs table (status, model used, personas used, timestamps)
  - results table (per-persona reviews, rating, register tier, sentiment)
  - Row Level Security policies on all tables

  3. Auth
  - Sign up page
  - Login page
  - Protected routes (redirect to login if unauthenticated)
  - Auth state in Zustand (session, user, org)
  - JWT verification middleware in FastAPI

  4. Dashboard (Home)
  - Projects list with last-run date + status
  - Empty state for new users
  - "New Project" button

  5. New Project Flow
  - Form: product name, description, category
  - Product image upload (Supabase Storage)
  - On submit: create project in DB + trigger panel run

  6. Panel Run & Results
  - Run status / live progress (Supabase real-time)
  - Results view: individual persona reviews
  - Aggregate: sentiment breakdown, top themes, register distribution
  - Re-run button

  7. Export
  - PDF report download
  - CSV export
  - Shareable read-only link

  8. History
  - Per-project: list of all past runs
  - Click into any past run to see its results
  
  9. Backend Updates (FastAPI)
  - Endpoints for project CRUD
  - Panel run creation + status updates
  - Write results to Supabase after run completes
  - Export generation endpoint

  ---
  That's roughly 9 areas of work. Want me to now write the formal spec doc and then build out
  the implementation plan?