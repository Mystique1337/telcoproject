# Naija Persona Agent — FastAPI runtime requirements
# For non-poetry installs: pip install -r requirements.txt

# Web framework
fastapi>=0.115,<0.117
uvicorn[standard]>=0.32,<0.34

# Schemas + config
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0

# Prompt templating
jinja2>=3.1

# HTTP client (used by LLM provider clients — Anthropic, OpenAI, NVIDIA, LM Studio)
httpx>=0.27,<0.29

# LLM SDKs
anthropic>=0.39
openai>=1.55

# Vector store (Task 2 product retrieval)
chromadb>=0.5,<0.6
pinecone>=7.0

# Data
pandas>=2.0
numpy>=1.24,<2

# HuggingFace datasets (used in scripts/ — comment out if not building product index)
# datasets>=3.0

# Demo (judge-facing UI)
streamlit>=1.38
requests>=2.32

# Evaluation suite (scripts/eval_all.py)
bert-score>=0.3.13
rouge-score>=0.1.2
scikit-learn>=1.3
# torch is a bert-score transitive dep — pinned for CPU stability on macOS
torch>=2.2,<3.0
