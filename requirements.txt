# Naija Persona Agent - FastAPI runtime requirements.
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.115,<0.117
uvicorn[standard]>=0.32,<0.34

# Schemas + config
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0

# Database (SQLAlchemy ORM + PostgreSQL driver)
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# Auth (Supabase passwordless + JWT verification)
supabase>=2.0,<3.0
python-jose[cryptography]>=3.3,<4.0

# Prompt templating
jinja2>=3.1

# HTTP client (used by every LLM provider client)
httpx>=0.27,<0.29

# Frontier LLM SDKs
anthropic>=0.39
openai>=1.55

# Vector retrieval
chromadb>=0.5,<0.6
pinecone>=7.0

# Data
pandas>=2.0
numpy>=1.24,<2

# Utilities (scripts / bulk import / human-eval XLSX)
openpyxl>=3.1,<4.0
pyyaml>=6.0,<7.0
tqdm>=4.66,<5.0
requests>=2.32

# Evaluation suite
bert-score>=0.3.13
rouge-score>=0.1.2
scikit-learn>=1.3
torch>=2.2,<3.0
